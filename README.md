# `infrastructure` - AWS Cluster & Cloud Provisioning

Note that during destruction, VPC clean-up may timeout due to internal
network interfaces created by AWS.

## Usage

```bash
terraform apply
```
